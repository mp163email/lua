---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by wangyu.
--- DateTime: 2019-2-21 10:35
--- lua中的bug和list差不多，与set相比可以放多个重复的值
--- 实现思想也是一个table,用key来表示元素，值表示元素个数： list = {zhang = 1, wang = 2, li = 1}, wang就是2个。这就要求每次插入和删除元素，要改变这个值

function insert (bag, element)
    bag[element] = (bag[element] or 0) + 1
end

function remove(bag, element)
    local count = bag[element]
    --bag[element] = (count and count > 1) and count - 1 or nil--如下：
    local final = nil
    if not count then
        if count > 1 then
            final = count - 1
        end
    end
    bag[element] = final
end