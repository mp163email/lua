---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by wangyu.
--- DateTime: 2019-2-15 17:12
---

---函数只是一种类型，跟传统类型一样，只不过用语法糖包装了一下，看着更像函数
---函数属于第一类值，跟传统类型一样，可以存储到变量或table中，也可以当参数或返回值
---当讨论一个函数时，实际是讨论的一个持有这个函数的变量


---[[

print('-----------')

---将一个函数作为参数的函数被称为高阶函数
---匿名函数做参数
tt = {
    {name = "a", age = 10},
    {name = "b", age = 20},
    {name = "c", age = 30},
}
table.sort(tt, function (a, b)--传递了一个匿名函数
    return a.name < b.name
end)
for i = 1, #tt do
    local intt = tt[i]
    print(intt.name)
end

---将函数作为返回值
---先验证2个参数是否正确，然后返回两数相乘的一个函数
function validate(a, b)
    if type(a) ~= "number" or type(b) ~= "number" then
        print("请输入数值")
        return
    end
    return function (a, b)
        return a * b
    end
end
local a, b = 100, 2
local cheng = validate(a, b)
print(cheng(a, b))
--]]