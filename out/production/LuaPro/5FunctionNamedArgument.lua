---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by wangyu.
--- DateTime: 2019-2-15 16:45
---
---具名实参: 封装成一个含有key的table, 这样传递变量的时候，就不仅仅有具体的值，还给值赋予了一个有意义的名字
---用table封装， 先把所有的参数包装成一个含有实际意义的key的table里，然后自己写一个函数，参数只有这个包装后的table，
---再在自己的函数里调用真正的函数
function Windown (options)
    if type(options.x) ~= number then
        print("x,应该为数值")
        return
    end
    print("Windown函数，做一些参数验证和默认值初始化")
    _Windown(options.title, options.x or 0, options.y)
end

function _Windown(...)
    print("_Windown函数是真正的运行函数")
end

local namedParams = { x = 0, y = 0, title = "lua" }--封装成一个含有key的table, 这样传递变量的时候，就不仅仅有具体的值，还给值赋予了一个有意义的名字

Windown(namedParams)